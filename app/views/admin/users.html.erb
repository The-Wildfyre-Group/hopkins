<script> if (typeof jQuery != 'undefined') { alert("jQuery library is loaded!"); }else{ alert("jQuery library is not found!"); } </script>
<br>
<br>
<br>
<br>
<%=link_to "Delete", User.last,  method: :delete, data: { confirm: "Are you sure you want to delete this user?"} %></td>
<table>
    <tr>
        <th>Name</th>
        <th>Email</th>
        <th>Surveys Completed</th>
        <th>Age</th>
        <th>Education</th>
        <th>State</th>
        <th>Manage</th>

    </tr>
    <% @users.each do |user|%>
    <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= "2/5" %></td>
        <td><%= user.age %></td>
        <td><%= user.education %></td>
        <td><%= user.state %></td>
        <td><%= link_to "Edit" %></td>
        <td><%= link_to "Delete", user, method: :delete, data: { confirm: "Are you sure you want to delete this user?"} %></td>

    </tr>
    <% end %>
</table>

